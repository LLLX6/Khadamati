# تقرير إصدار خدماتي v65

## نطاق الإصدار

هذا إصدار إصلاحي محدود يعالج ثلاث نقاط فقط:

1. توزيع صفحة البداية على كامل مساحة الهاتف مع دعم المساحات الآمنة في iPhone.
2. استعادة حساب المستخدم أو المزود بعد إغلاق التطبيق وإعادة فتحه.
3. التبديل المباشر بين الحسابين المحفوظين على الجهاز دون إعادة إدخال الاسم أو الهاتف أو رمز الدخول.

## الأمان والخصوصية

- لا يُحفظ رمز الدخول داخل ذاكرة الحسابات.
- تُحفظ هوية مختصرة لازمة لاستعادة الواجهة فقط.
- يظل تسجيل الخروج الصريح وحذف الحساب وانتهاء الجلسة أسبابًا صحيحة لطلب تسجيل الدخول مجددًا.
- جلسة تسجيل المزود قيد المراجعة أصبحت قابلة للاستعادة، مع صلاحية محدودة للحساب نفسه.

## نتائج التحقق

- اختبارات Python: نجح 30 من 30.
- اختبار رحلة API المعزولة: نجح.
- اختبار واجهة الهاتف 390x844: نجح، ويشمل إغلاق الصفحة وإعادة فتحها والتبديل بين المستخدم والمزود.
- اختبار الأمان: نجح، ويشمل العزل والصلاحيات والجلسات والملفات وقيود الطلبات.
- فحص JavaScript وبناء الخادم: نجح.
- فحص المستودع: نجح دون أخطاء أو تحذيرات.
- تطابق `index.html` و`service-worker.js` وملف تنسيق v65 مع نسخ `public`: مؤكد.

## ملاحظة تشغيلية

تستمر الجلسة بحسب قيمة `KHADAMATI_SESSION_DAYS` في الخادم، وقيمتها الافتراضية 30 يومًا. لا يُطلب رمز الدخول مجددًا خلال الجلسة السليمة إلا بعد تسجيل الخروج أو حذف الحساب أو انتهاء الجلسة أو إبطالها أمنيًا.
