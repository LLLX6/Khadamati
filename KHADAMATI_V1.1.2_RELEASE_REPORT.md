# تقرير إصدار خدماتي V1.1.2

تاريخ التحقق: 2 أغسطس 2026  
Commit التنفيذ: `07035888434fd52e6e6d38eb16353eedb5c9e03e`  
الفرع: `main`  
الإصدار: `v1.1.2`

## الملخص التنفيذي

يعالج هذا الإصدار الوصول إلى حساب الإدارة واستعادة رموز المستخدمين والمزودين، ويحسن تسجيل المزود وتجربة المجتمع وتوفر الخدمات وإرشاد المزود لأول مرة. التغييرات منشورة على GitHub Pages وRender، مع الحفاظ على التوافق مع البيانات القديمة وإضافة تغييرات قاعدة بيانات تراكمية فقط.

## ما تم تنفيذه

- إضافة مسار آمن لإرسال رمز إدارة مؤقت من ستة أرقام إلى بريد خدماتي، مع صلاحية محدودة ومحاولات محدودة واستخدام مرة واحدة.
- إبقاء رمز الإدارة المعتاد مسارًا احتياطيًا عند غياب إعدادات البريد، من دون عرض أي سر داخل الواجهة أو المستودع.
- تحويل استعادة حساب المستخدم أو المزود إلى ثلاث خطوات: إرسال الرمز، التحقق منه، ثم اختيار رمز دخول جديد.
- تخزين رموز البريد وReset Tokens بصيغة Hash، وإلغاء الجلسات القديمة بعد تغيير رمز الدخول.
- تحسين واجهات الإدارة والاستعادة وتسجيل الشركة والمجتمع للشاشات الصغيرة ومنع تكبير Safari عند التركيز على الحقول.
- تقييد حجم فريق الشركة وفق الباقة في الواجهة والخادم، وإجبار حساب الفرد على عضو واحد.
- جعل إجراءات التسجيل في نهاية النموذج بدل بقائها فوق المحتوى أثناء التمرير.
- جعل الخدمات قابلة للطلب حتى عند عدم وجود مزود متاح لحظيًا؛ يُحفظ الطلب في المطابقة بدل تعطيل الخدمة.
- إضافة أدلة استخدام سياقية للمزود في «اليوم» و«المجتمع» و«مهامي» و«حسابي» مع صور عمانية مناسبة للسياق.
- تحديث الخصوصية والشروط وجدول البيانات المجمعة بما يغطي البريد ورموز الاستعادة المؤقتة.
- تحديث Manifest وService Worker وCache Version وبيانات الإصدار إلى `1.1.2`.

## قاعدة البيانات والترحيل

التغييرات تراكمية ولا تحذف بيانات:

- إضافة `verified_at` و`reset_token_hash` إلى `password_recoveries`.
- إضافة جدول `admin_email_challenges` وفهرس انتهاء الصلاحية.
- تنفيذ `ensure_column` لضمان توافق قواعد SQLite القديمة.
- تحديث `postgres_schema.sql` ليتطابق مع المخطط الجديد.
- لا توجد Migration هادمة أو إعادة إنشاء لقاعدة البيانات.

## الأمان

- الرموز المؤقتة عشوائية من ستة أرقام، قصيرة الصلاحية، ومخزنة كـHash.
- منع إعادة استخدام الرمز وتحديد عدد المحاولات والطلبات.
- الاستجابة لطلب استعادة حساب غير موجود لا تكشف وجود الحساب.
- إلغاء جلسات الحساب بعد نجاح تعيين رمز دخول جديد.
- فحص الحزم: صفر ثغرات معلنة في `npm audit` وقت الإصدار.
- فحوص IDOR وCORS وتوقيع الوسائط وحماية PIN وMIME وحجم الملفات واجتياز عزل الزائر نجحت.

## نتائج الاختبارات

- Unit/API Python: `66/66` ناجحة.
- Smoke API المعزول: جميع المسارات ناجحة، بما فيها التسجيل والاستعادة والجلسات والمطابقة والمجتمع والمحادثات والصوت والاشتراكات والإدارة.
- Security API: جميع الفحوص ناجحة.
- JavaScript syntax check: ناجح.
- UI End-to-End: ناجح للمستخدم والطلب والمزود والإدارة وملاءمة الهاتف.
- Repository verification: ناجح، `280` ملفًا متتبعًا، بلا أخطاء أو تحذيرات.
- تطابق ملفات `root/public`: ناجح.
- فحص `git diff --check`: لا توجد أخطاء مسافات؛ ظهرت تنبيهات CRLF الخاصة بويندوز فقط.

## الأداء المحلي

- DOMContentLoaded: `113ms`.
- Load: `119ms`.
- First Contentful Paint: `164ms`.
- Interactive: `1335ms`.
- البيانات المنقولة: `1,241,062 bytes`.
- الاختبار منخفض الحمل ومحلي، ولا يمثل ضمانًا لسعة Render الإنتاجية.

## الفحص البصري

تم التقاط وفحص واجهات الدخول والاستعادة وتسجيل الشركة وإجراءات التسجيل وأدلة المزود والمجتمع على الهاتف. لم يظهر تمرير أفقي أو خروج لعناصر الواجهات المستهدفة.

## الملفات والأصول

أهم الملفات المعدلة:

- `server.py`, `postgres_schema.sql`, `khadamati_domain.py`.
- `index.html`, `assets/styles/khadamati-v1.css` ونسخهما داخل `public/`.
- `manifest.webmanifest`, `service-worker.js` ونسخهما داخل `public/`.
- `render.yaml`, `.env.example`, `README.md`, `RELEASE_NOTES.md`.
- اختبارات الاستعادة وAPI والأمان وواجهة المستخدم.
- سياسات الخصوصية والشروط وجدول البيانات بالعربية والإنجليزية.

أزيلت أربعة أصول إرشادية قديمة غير مستخدمة واستبدلت بأربعة أصول WebP سياقية محسنة داخل `assets/` و`public/assets/`.

## النشر والتحقق الحي

- GitHub Pages: `https://lllx6.github.io/Khadamati/`.
- Render API: `https://khadamati-app-api.onrender.com`.
- Health Check أعاد `ok: true` و`release: v1.1.2`.
- Render نشر Commit التنفيذ بحالة `live`، Deploy ID: `dep-d9n6un0ae00c73f47s40`.
- الواجهة الحية أعادت HTTP 200 وتحتوي Build `email-access-r1` والإصدار `1.1.2`.

## متغيرات البيئة المطلوبة للبريد

الأسماء فقط، ولا توجد قيم سرية في المستودع:

- `KHADAMATI_ADMIN_EMAIL`
- `KHADAMATI_SMTP_HOST`
- `KHADAMATI_SMTP_PORT`
- `KHADAMATI_SMTP_USER`
- `KHADAMATI_SMTP_PASSWORD`
- `KHADAMATI_SMTP_FROM_EMAIL`
- `KHADAMATI_SMTP_FROM_NAME`
- `KHADAMATI_SMTP_USE_TLS`
- `KHADAMATI_SMTP_USE_SSL`

## قيد خارجي واضح

اختبار النسخة الحية أعاد `email_delivery_unavailable`: إعدادات Gmail السرية (`KHADAMATI_SMTP_USER` و`KHADAMATI_SMTP_PASSWORD`) غير مفعلة على Render. لذلك يعمل رمز الإدارة المعتاد كمسار احتياطي، وتبقى استعادة الحساب في المراجعة اليدوية عند تعذر البريد/واتساب. لتفعيل الإرسال المجاني يجب على مالك بريد خدماتي تفعيل التحقق بخطوتين وإنشاء Gmail App Password ثم إدخاله سرًا في Render؛ لا يمكن إنشاء هذا السر أو استخراجه من الكود.

## الحكم النهائي

الإصدار البرمجي منشور ويعمل، وجميع الاختبارات المحلية والخلفية المكتملة ناجحة. إرسال البريد الحقيقي جاهز تقنيًا لكنه يحتاج إدخال سر Gmail الخارجي المذكور أعلاه، ثم اختبار رسالة فعلية واحدة.
