# إصلاح دخول الإدارة — 11–12 سبتمبر 2026

## سبب المتابعة

أكد المالك أن البيانات الحالية تجريبية وأبلغ أن رمز الإدارة لا يفتح. أظهرت سجلات خدمة الاختبار `khadamati-launch-check-20260911` محاولتي `POST /api/admin/login` مرفوضتين بحالة 403 عند 22:46 UTC. لا تكشف هذه السجلات قيمة الرمز أو السبب التفصيلي للرفض.

أُنشئت خدمة الاختبار برمز إدارة عشوائي مستقل في إعدادات Render؛ لم يُنقل رمز الخدمة القديمة إليها. لم يُسلّم الرمز الجديد ضمن الرد السابق، وهذا سبب محتمل لتعذر الدخول بالرمز المعتاد. قيمة `KHADAMATI_ADMIN_CODE` موجودة في قسم Environment لخدمة الاختبار، وليست في المستودع أو هذا التقرير. يُستخدم هذا المتغير لإنشاء أول حساب إدارة في قاعدة فارغة؛ تغييره وحده لا يغير رمز حساب موجود.

## الخلل المثبت والإصلاح

كان مسار البريد في الواجهة يفترض أن نجاح الخطوة الأولى يعني اكتمال الدخول، بينما قد يطلب الخادم إعداد تطبيق المصادقة أو إدخال رمزه. أدى ذلك إلى محاولة فتح جلسة إدارة قبل إصدار رمز جلسة. لم يكن هذا الخلل تجاوزًا لصلاحيات الخادم، ولا يُدّعى أنه سبب كل رفض لرمز الإدارة.

- توحيد معالجة نتائج الدخول بالبريد وبرمز الإدارة.
- الاحتفاظ ببيانات الخطوة الأولى في الذاكرة أثناء التحقق الثنائي فقط، وإرسالها مع رمز التحقق، بما يشمل إعادة المحاولة بعد رمز خاطئ.
- عرض إعداد المصادقة عند طلب الخادم، ثم عرض رموز الاسترداد بعد نجاح الإعداد.
- مسح بيانات الخطوة السابقة عند الإلغاء أو اكتمال الدخول، ورفض تفعيل جلسة من استجابة لا تحتوي رمز جلسة جديدًا.
- مزامنة `index.html` و`public/index.html`.
- إضافة سبعة اختبارات لسلوك معالجات الدخول الفعلية وربطها ببوابة الجودة، وتوسيع اختبار الواجهة ليشمل انتقال كل من الرمز والبريد إلى المصادقة.

لم تُغيّر كلمات مرور الحسابات أو إعدادات التوثيق الثنائي. لم يُضف رمز ثابت أو باب دخول بديل، ولم تُنفذ عملية حذف بيانات يدويًا. تأكيد المالك بشأن نوع البيانات لا يُعامل كطلب لمسحها؛ أثر التخزين المؤقت عند النشر موضح أدناه.

## التحقق المحلي

| الفحص | النتيجة |
| --- | --- |
| `node --test tests/admin-auth.test.js` | 7 ناجحة؛ تشمل البريد والرمز، رمز تحقق خاطئ وإعادة المحاولة، إعداد المصادقة، الإلغاء، ورفض الاستجابة الناقصة |
| تشغيل اختبارات الانحدار على الواجهة السابقة في مجلد معزول | أعاد إظهار فشل مساري التحقق والإعداد عبر البريد وغياب حماية الاستجابة الناقصة |
| تجميع ملفات Python | نجح |
| `python3 -m unittest discover -s tests -p 'test_*.py' -q` | 178 ناجحًا |
| `python3 tests/security-api.py` | جميع فحوص الأمان ناجحة |
| `python3 tests/run-smoke-isolated.py` | جميع فحوص API المعزولة ناجحة |
| عقد الترجمات | 5 لغات و4919 صفًا |
| مزامنة الملفات والتحقق من المستودع | نجحا؛ لا أخطاء أو تحذيرات في فحص المستودع |

تعذر استكمال تثبيت اعتماديات بيئة Python جديدة بسبب الاتصال؛ نجحت الفحوص المحلية باستخدام Python المتاح. تتحقق بوابة GitHub من الاعتماديات المثبتة من الملفات المقفلة. اختبارات الواجهة والمحاكاة المحلية تستخدم بيانات مصطنعة، ولا تثبت نجاح الدخول إلى الحساب المنشور.

## التحقق من الرابط والنشر

فُتحت [نسخة الاختبار](https://khadamati-launch-check-20260911.onrender.com/) بمتصفح فعلي، ثم أُعيد تحميلها بعد نشر الإصلاح. ظهرت الشاشة الرئيسية وخيارات الإدارة: إرسال رمز بريد أو استخدام رمز الإدارة. لم يكتمل دخول المالك حتى الآن؛ لا تُستنتج سلامة المصادقة الفعلية من ظهور الصفحة.

نتائج GitHub والنشر المؤكدة في 12 سبتمبر:

- الالتزام المختبر والمنشور: `adb7ca65afb1b8c5bb5d2b0f3c407c79e9c6017d`، ضمن [طلب التغييرات 11](https://github.com/LLLX6/Khadamati/pull/11).
- نجحت [بوابة الجودة](https://github.com/LLLX6/Khadamati/actions/runs/34682182664) كاملة: الخادم والأمان والاعتماديات وسبعة اختبارات دخول جديدة، والترجمات، والواجهة على المقاسات 320×740 و375×812 و390×844 و430×932 و1280×800. سجل فحص الأداء المحلي جاهزية التفاعل خلال 1632ms؛ هذا قياس CI وليس قياس شبكة المستخدم.
- نجحت [بوابة الهاتف](https://github.com/LLLX6/Khadamati/actions/runs/34682182684) كاملة: الجسر والواجهة المضمّنة، وAndroid، وiOS للمحاكي. تتضمن ملفاتها APK داخليًا وAAB غير موقّع وتطبيق iOS للمحاكي، وليست إصدارًا منشورًا في المتاجر. تنتهي صلاحية ملفات هذا التشغيل في 26 سبتمبر 2026. عنوان API الافتراضي للهاتف لا يزال الخدمة الأصلية، وليس خدمة الاختبار.
- نشر Render: `dep-daigh267bikc738ikv70`، الحالة `live` منذ `2026-09-12T08:09:32Z`، على خدمة الاختبار `srv-dai5059594qs73efnmi0` فقط.
- أكدت سجلات بدء النسخة الجديدة بيئة `staging` والإصدار `1.3.1`، واستجابة `HEAD /` و`GET /` بحالة 200. لم يُرجع استعلام أخطاء التشغيل من بدء النشر إلى `08:10:23Z` أخطاء؛ لا يُعد ذلك ضمانًا لغياب جميع الأعطال.
- لم تُنفذ عملية حذف بيانات يدويًا، لكن نشر خدمة Free يعيد إنشاء التخزين المؤقت؛ نُبّه المالك لذلك قبل النشر. لا تُستخدم هذه البيئة لحفظ بيانات إطلاق حقيقية.

تظل خدمة `khadamati-app-api` وفروع `main` والإنتاج على حالها. لم يُغيّر رمز الإدارة، ولم تُستخرج قيم الأسرار من Render. يلزم إدخال المالك الرمز الصحيح وإكمال المصادقة للتحقق من الحساب المنشور؛ لا تُرسل قيم الرموز في المحادثة. حسابا المتاجر والتوقيع والتخزين الدائم واختبار الأجهزة الفعلية ما زالت خطوات مستقلة قبل الإطلاق العام.

متابعة الدخول الفعلي: اختار المالك مسار رمز الإدارة وأرسله عبر نموذج إدخال آمن لا يكشف قيمته للمساعد. ظلت شاشة الدخول ظاهرة، وسجل Render رفض `POST /api/admin/login` بحالة 403 عند `2026-09-12T08:56:16Z`. لم تظهر رسالة خطأ في فحص الواجهة اللاحق؛ لا تُنسب هذه المحاولة إلى نجاح المصادقة أو إلى الخلل المنفصل الذي أُصلح في البريد. يلزم إكمال الدخول بالرمز الصحيح عبر المالك، ولم تُكرر المحاولة تلقائيًا أو تُعطّل الحماية.
