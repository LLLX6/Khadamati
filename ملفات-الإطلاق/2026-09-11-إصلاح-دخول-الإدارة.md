# إصلاح دخول الإدارة — 11 سبتمبر 2026

## سبب المتابعة

أكد المالك أن البيانات الحالية تجريبية وأبلغ أن رمز الإدارة لا يفتح. أظهرت سجلات خدمة الاختبار `khadamati-launch-check-20260911` محاولتي `POST /api/admin/login` مرفوضتين بحالة 403 عند 22:46 UTC. لا تكشف هذه السجلات قيمة الرمز أو السبب التفصيلي للرفض.

أُنشئت خدمة الاختبار برمز إدارة عشوائي مستقل في إعدادات Render؛ لم يُنقل رمز الخدمة القديمة إليها. لم يُسلّم الرمز الجديد ضمن الرد السابق، وهذا سبب محتمل لتعذر الدخول بالرمز المعتاد. قيمة `KHADAMATI_ADMIN_CODE` موجودة في قسم Environment لخدمة الاختبار، وليست في المستودع أو هذا التقرير. يُستخدم هذا المتغير لإنشاء أول حساب إدارة في قاعدة فارغة؛ تغييره وحده لا يغير رمز حساب موجود.

## الخلل المثبت والإصلاح

كان مسار البريد في الواجهة يفترض أن نجاح الخطوة الأولى يعني اكتمال الدخول، بينما قد يطلب الخادم إعداد تطبيق المصادقة أو إدخال رمزه. أدى ذلك إلى محاولة فتح جلسة إدارة قبل إصدار رمز جلسة. لم يكن هذا الخلل تجاوزًا لصلاحيات الخادم، ولا يُدّعى أنه سبب كل رفض لرمز الإدارة.

- توحيد معالجة نتائج الدخول بالبريد وبرمز الإدارة.
- الاحتفاظ ببيانات الخطوة الأولى في الذاكرة أثناء التحقق الثنائي فقط، وإرسالها مع رمز التحقق، بما يشمل إعادة المحاولة بعد رمز خاطئ.
- عرض إعداد المصادقة عند طلب الخادم، ثم عرض رموز الاسترداد بعد نجاح الإعداد.
- مسح بيانات الخطوة السابقة عند الإلغاء أو اكتمال الدخول، ورفض تفعيل جلسة من استجابة لا تحتوي رمز جلسة جديدًا.
- مزامنة `index.html` و`public/index.html`.
- إضافة سبعة اختبارات لسلوك معالجات الدخول الفعلية وربطها ببوابة الجودة، وتوسيع اختبار الواجهة ليشمل انتقال كل من الرمز والبريد إلى المصادقة.

لم تُغيّر كلمات مرور الحسابات أو إعدادات التوثيق الثنائي. لم يُضف رمز ثابت أو باب دخول بديل، ولم تُحذف البيانات التجريبية. تأكيد المالك بشأن نوع البيانات لا يُعامل كطلب لمسحها.

## التحقق المحلي

| الفحص | النتيجة |
| --- | --- |
| `node --test tests/admin-auth.test.js` | 7 ناجحة؛ تشمل البريد والرمز، رمز تحقق خاطئ وإعادة المحاولة، إعداد المصادقة، الإلغاء، ورفض الاستجابة الناقصة |
| تشغيل اختبارات الانحدار على الواجهة السابقة في مجلد معزول | أعاد إظهار فشل مساري التحقق والإعداد عبر البريد وغياب حماية الاستجابة الناقصة |
| تجميع ملفات Python | نجح |
| `python3 -m unittest discover -s tests -p 'test_*.py' -q` | 178 ناجحًا |
| `python3 tests/security-api.py` | جميع فحوص الأمان ناجحة |
| `python3 tests/run-smoke-isolated.py` | جميع فحوص API المعزولة ناجحة |
| عقد الترجمات | 5 لغات و4919 صفًا |
| مزامنة الملفات والتحقق من المستودع | نجحا؛ لا أخطاء أو تحذيرات في فحص المستودع |

تعذر استكمال تثبيت اعتماديات بيئة Python جديدة بسبب الاتصال؛ نجحت الفحوص المحلية باستخدام Python المتاح. تتحقق بوابة GitHub من الاعتماديات المثبتة من الملفات المقفلة. اختبارات الواجهة والمحاكاة المحلية تستخدم بيانات مصطنعة، ولا تثبت نجاح الدخول إلى الحساب المنشور.

## التحقق من الرابط والنشر

فُتحت [نسخة الاختبار](https://khadamati-launch-check-20260911.onrender.com/) بمتصفح فعلي. ظهرت الشاشة الرئيسية وخيارات الإدارة: إرسال رمز بريد أو استخدام رمز الإدارة. لم يكتمل دخول المالك حتى الآن؛ لا تُستنتج سلامة المصادقة الفعلية من ظهور الصفحة.

تُسجّل نتيجة بوابات GitHub ومعرّف نشر الإصلاح هنا بعد اكتمالهما. تُنشر هذه الجولة على خدمة الاختبار فقط. تظل خدمة `khadamati-app-api` وفروع `main` والإنتاج على حالها. حسابا المتاجر والتوقيع والتخزين الدائم واختبار الأجهزة الفعلية ما زالت خطوات مستقلة قبل الإطلاق العام.
