# تقرير تحديث خدماتي V1.1.3

تاريخ التحقق: 2 أغسطس 2026  
فرع العمل: `fix/subscriptions-chat-workflow-v1.1.3`

## ملخص التنفيذ

تم تجهيز تحديث متوافق مع البيانات السابقة يركز على فصل باقات الأفراد والشركات، تطبيق حدود الباقات من الخادم، تحسين رحلة العروض والاتفاق داخل المحادثة، وضبط الحالات البصرية والتنقلات المطلوبة على الهاتف.

## التغييرات المنفذة

- إضافة أربع باقات مستقلة للفرد وأربع للشركة: مجانية، فضية، ذهبية، ونخبة.
- تطبيق مدة الاشتراك، الأقسام، الخدمات، المحافظات، الولايات، صور الأعمال، باقات المجتمع، مدة الإعلان، وتأخير ظهور الطلبات بحسب الباقة.
- دعم تعديل الباقات وإيقافها من الإدارة مع توافق معرفات الباقات القديمة.
- ربط خدمات التسجيل ونطاق التغطية وفريق الشركة بمصدر الخادم وحدود الباقة.
- اعتماد وقت الخادم لترتيب الإشعارات ووقت نشر طلبات المجتمع من الأحدث إلى الأقدم.
- تمديد الجلسة بصورة آمنة وتجديدها قبل انتهائها بدل إخراج الحساب أثناء الاستخدام.
- إخفاء إضافة مزود معروف بعد بدء المقارنة، والحفاظ على موضع المقارنة عند فتح ملف المزود والرجوع.
- نقل العرض والاتفاق إلى بطاقة داخل المحادثة: المرسل مؤكد تلقائيًا، والمستلم يقبل أو يطلب تعديلًا، مع سجل إداري وتنزيل نسخة نصية.
- إصلاح إعادة تحميل رابط الصوت الخاص عند انتهاء توقيعه وإظهار إعادة المحاولة للطرف المستلم.
- إظهار صور طلب المستخدم للمزود مع معاينة كاملة.
- مشاركة باقة المجتمع كصورة بطاقة ورابط عميق يعيد إلى الباقة نفسها.
- إظهار اسم الشركة كاملًا وتنظيم المفضلة والمشاركة والإبلاغ.
- تمييز حالات الطلب بالألوان، وإبراز الأقسام ذات المزود المتاح.
- تحسين خطوة موقع الطلب المباشر وإضافة زر رجوع عربي صحيح بجوار العنوان.
- استعادة زر بحث المجتمع في الوضع الفاتح والداكن والعربية والإنجليزية.
- تحديث الإصدار والكاش والـManifest وREADME إلى `1.1.3`.

## قاعدة البيانات والترحيل

- التغييرات الإضافية لا تعيد إنشاء قاعدة البيانات ولا تحذف السجلات الحالية.
- معرفات الباقات القديمة تُحوّل إلى أقرب باقة جديدة متوافقة مع نوع الحساب.
- أضيفت الحقول والجداول المطلوبة بصورة قابلة للتشغيل المتكرر دون تكرار البيانات.
- لم يُنفذ أي ترحيل على قاعدة إنتاج ولم تُحذف أي بيانات حقيقية.

## نتائج التحقق

- `python -m unittest tests.test_domain tests.test_community`: نجح `40/40`.
- `python tests/run-smoke-isolated.py`: نجحت رحلات API المعزولة، بما فيها الاشتراكات والمجتمع والمحادثة والصوت والصلاحيات.
- `npm run test:ui`: نجحت رحلات المستخدم والطلب والمزود والإدارة وملاءمة الهاتف.
- `npm run check:js`: نجح.
- `python -m py_compile server.py khadamati_domain.py khadamati_community.py khadamati_workflow.py`: نجح.
- فحص JavaScript المضمن في `index.html`: نجح لملف واحد.
- `git diff --check`: لا توجد أخطاء مسافات؛ ظهرت تنبيهات Windows الخاصة بتحويل LF إلى CRLF فقط.
- فحص الإضافات بحثًا عن تعيينات أسرار واضحة: صفر نتائج.
- تطابق `root/public`: نجح للواجهة وCSS والـManifest والـService Worker.

## التحقق البصري

تم إنشاء 43 لقطة على عرض `390x844` داخل `outputs/v1.1.3-smoke-390`. شملت الدخول، التسجيل، الرئيسية، الخدمات، الطلب المباشر، المقارنة، المحادثة، حساب المزود، الإدارة، المجتمع، العربية، الإنجليزية، والوضع الداكن.

## القيود والإجراءات الخارجية

- لم يتم دمج الفرع في `main` ولم يتم النشر العام أو تغيير Render، التزامًا بقواعد المستودع.
- استمرار الملفات والبيانات بعد إعادة تشغيل Render يعتمد على وجود قاعدة بيانات وتخزين دائمين في البيئة الفعلية، ويجب التحقق منهما قبل إطلاق إنتاجي.
- لم تُفعّل أي خدمة مدفوعة ولم تُضف أسرار حقيقية.
- يلزم تشغيل ترحيل قاعدة البيانات أولًا في بيئة staging مع نسخة احتياطية واختبار استعادة قبل تطبيقه على production.

