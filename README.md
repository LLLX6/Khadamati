# خدماتي | Khadamati App

منصة عمانية تربط العملاء بمزودي الخدمات والشركات، وتدير الطلبات والعروض والمحادثات والاشتراكات من واجهة عربية وإنجليزية مهيأة للهاتف أولاً.

## الإصدار v66

- واجهة هاتف موحدة بهدوء بصري أكبر، وشبكة 8px، وزوايا وأهداف لمس متناسقة من عرض 320px.
- تنقل المستخدم أصبح: الرئيسية، الساحة، مهامي، المحادثات، حسابي؛ والخدمات والبحث متاحان من موضعهما الطبيعي دون تكرار.
- مركز «مهامي» يجمع الطلبات الجارية والسابقة ويعرض لكل طلب إجراءً تاليًا واحدًا وفق مرحلته.
- جواز مختصر لكل طلب يعرض الخدمة والموقع والحالة والطرف المختار، مع التفاصيل الثانوية داخل قائمة منسدلة.
- لا يمكن بدء التنفيذ قبل اتفاق مؤكد من الطرفين، ولا تبقى المقارنة أو إعادة العرض بعد اختيار المزود.
- صفحة محادثات مستقلة تفتح الطرف والطلب الصحيحين، مع بقاء الإشعارات للتحديثات والطلبات فقط.
- بطاقات المزودين تحافظ على ظهور الصورة كاملة؛ تستخدم تدرجًا شفافًا على الهاتف وجسمًا واضحًا أسفل الصورة على الكمبيوتر.
- نقاط الولاء لا تعد بجائزة ثابتة؛ يمكن للإدارة تفعيل حملة وتحديد هدفها واسمها ووصفها بالعربية والإنجليزية.
- طبقة التصميم `assets/styles/khadamati-v66.css` وCache باسم `khadamati-app-shell-v66-vision-2030`.
- البرومبت التنفيذي المعتمد موجود في `KHADAMATI_V66_VISION_2030_EXECUTION_PROMPT_AR.md`.

### التحسينات المستمرة من v65

- إصلاح استعادة حساب المستخدم والمزود بعد إغلاق التطبيق بالكامل، مع الاحتفاظ بجلسة كل دور مدة صلاحيتها وعدم تخزين رمز الدخول.
- تبديل مباشر بين الحسابين المحفوظين على الهاتف، مع عرض البيانات المحفوظة فورًا ثم مزامنتها من الخادم في الخلفية.
- إنشاء جلسة محفوظة لمزود الخدمة فور إرسال طلب التسجيل، حتى تبقى حالة «قيد المراجعة» بعد إعادة فتح التطبيق.
- إعادة توزيع شاشة البداية على كامل ارتفاع الهاتف بدل تكديس المحتوى في المنتصف وترك فراغات كبيرة.
- طبقة التصميم السابقة `assets/styles/khadamati-v65.css` تظل ضمن App Shell للتوافق مع طبقات الإصدار المتراكمة.

### التحسينات المستمرة من v64

- تسجيل المزود الفردي يدعم ثلاث خدمات مترابطة داخل قسم واحد، وتسجيل الشركة يدعم ست خدمات موزعة على ثلاثة أقسام في فترة التأسيس، مع فرض الحدود نفسها في الخادم.
- حدود الخدمات والأقسام والصور والولايات أصبحت مرتبطة بنوع الحساب والباقة، وتظل قابلة للتعديل من الإدارة من دون استبدال القيم المحفوظة.
- إلزام إرفاق وجهي بطاقة الهوية أو العمل للمراجعة السرية، وحد صور الأعمال إلى 5 للمزود الفردي و10 للشركة.
- ملفات المنازل والمركبات والأجهزة تعرض حقولاً مناسبة لنوع الأصل، وتُزال إحداثيات العميل وموافقات التواصل عند إغلاق العمل حفاظاً على الخصوصية.
- سجل أعمال للمزود يفصل المنجز وغير المنجز، مع CSV وطباعة أو حفظ PDF وإخفاء محلي قابل للاستعادة من العرض.
- حذف إداري مضبوط للمزود بعد إيقافه، مع تأكيد السبب وإبطال الجلسات وإخفاء البيانات وتسجيل العملية في Audit Log.
- إدارة التقييمات من مركز الجودة، مع حذف إداري موثق واستبعاد التقييم المحذوف من العرض العام والحسابات.
- تحسين الاتفاقية والتقييم بعد الإنجاز، والمحادثة الصوتية ومعاينتها، وأدوات رأس المحادثة والموقع والردود السريعة.
- التحكم بمشاركة الموقع الحي وإيقافه حتى نهاية اليوم، وتحسين الفلاتر المرئية للأقسام والخدمات الموقوفة في الإدارة.
- دليل مصور أول مرة لمسار «طلب مباشر»، وتحسينات هاتف للبطاقات والنوافذ والتمرير من عرض 320px.
- طبقة التصميم `assets/styles/khadamati-v64.css` وCache باسم `khadamati-app-shell-v64-final-workflow`.

### التحسينات المستمرة من v63

- دورة طلب موحدة تسجل الأحداث من الإنشاء والمطابقة حتى الاتفاق والتنفيذ وتأكيد العميل أو مراجعة الجودة.
- اتفاق تنفيذ واضح داخل الطلب للموعد والمدة والسعر، ويمنع بدء العمل ما دامت نسخته تنتظر تأكيد أحد الطرفين.
- توثيق إنجاز بصور قبل/بعد وقرار صريح من العميل، مع ربط بلاغ الجودة بالطلب والأدلة بدل إغلاق شكلي.
- ملفات خدمة اختيارية للمنازل والمركبات والأجهزة، تعرض سجل الطلبات المرتبط بكل أصل من دون تكرار البيانات.
- منع ازدواج الطلب عند إعادة الإرسال، وإعادة الطلب المنتهي كطلب جديد مستقل يحافظ على الخدمة والموقع فقط.
- سعة يومية للمزود تدخل في المطابقة، ومؤشرات تشغيلية للمزود وفجوات طلب/عرض عملية للإدارة.
- تحسينات هاتف إضافية لدورة الطلب وبطاقات المهام ورأس المزود من عرض 320px، مع طبقة `assets/styles/khadamati-v63.css`.
- Cache جديد باسم `khadamati-app-shell-v63-request-workflow` حتى تصل ملفات الإصدار الحالية إلى الأجهزة المثبت عليها التطبيق.

### التحسينات المستمرة من v62

- مساحة مزود مضغوطة للهاتف مع فصل المحادثات والتنبيهات والمهام الجارية.
- تبديل محفوظ وآمن بين حساب المستخدم وحساب المزود على الجهاز نفسه دون خلط بيانات الدورين.
- حذف التنبيهات المقروءة داخل القسم الحالي فقط، مع عدادات مستقلة للمحادثات والطلبات والتحديثات.
- إبقاء المستخدم داخل المحادثة عند إغلاق اختيار طرق التواصل أو المرفقات.
- تحسين شريط المحادثة والتسجيل الصوتي ومساحات الهاتف من عرض 320px.
- فصل إشعارات الإدارة عن إشعارات المستخدمين والمزودين على مستوى الخادم.
- Health وReadiness checks مستقلة، ومنع زرع بيانات العرض في قواعد الإنتاج الجديدة.
- أدوات فحص للمستودع وقاعدة SQLite ونسخ احتياطي واستعادة قابلة للتحقق.
- Quality Gate غير ناشر على GitHub Actions؛ لا يدمج ولا ينشر التطبيق تلقائيًا.

- فصل جرس لوحة الإدارة عن مركز إشعارات المستخدم والمزود؛ يفتح الآن مركز الإدارة مباشرة ويعرض عدده الصحيح.
- حفظ جلسة المستخدم وجلسة المزود على الجهاز نفسه مع دور نشط واضح، بحيث لا تختفي طلبات المستخدم عند العودة من بوابة المزود.
- توصيل الطلب المطابق فورًا للمزود عند تعطيل الاشتراكات، وتطبيق تأخير الباقة فقط عندما تفعّل الإدارة الاشتراكات.
- إضافة تاريخ ووقت إنشاء الطلب بصيغة 12 ساعة في بطاقات المستخدم والمزود والإدارة.
- منع ظهور صفحة «بوابة المزود» الناقصة أثناء استعادة الجلسة، واستبدالها بحالة تحميل هيكلية واضحة.
- تصغير طبقة نص الإعلان وجعلها شفافة وبحجم المحتوى حتى لا تحجب صورة الإعلان على الهاتف.
- معرض أعمال فعلي يفتح الصور بحجم كامل، يدعم التنقل بينها، ويغلق إلى بطاقة المزود نفسها من دون فقد موضع المستخدم.
- معاينة تقارير داخل التطبيق مع زر إغلاق ثابت وطباعة PDF، فلا يعلق المستخدم داخل صفحة التقرير.
- ترجمة نظامية لإشعارات المزود المولدة من الخادم في العربية والإنجليزية، مع سلامة RTL وLTR.
- استرجاع آمن لرمز الدخول عبر طلب إداري ورمز مؤقت أحادي العرض ورسالة واتساب جاهزة، من دون كشف الرمز الأصلي المخزن كقيمة مشفرة.
- إصلاح اختيار حساب المزود الصحيح عندما يوجد أكثر من سجل تاريخي للهاتف نفسه، مع بقاء حساب المستخدم والمزود منفصلين.
- توصيل الطلبات للمزودين المطابقين فعليًا، ورسائل نجاح دقيقة تميز بين الإرسال الفوري والانتظار حسب أولوية الباقة.
- تفعيل الباقة من الإدارة ينعكس على حساب المزود وصلاحياته، وطلب الترقية من المزود يصل إلى مركز إشعارات الإدارة.
- محرر الصور يدعم التصغير حتى إظهار الصورة كاملة، ويملأ الفراغ بنسخة ضبابية من الصورة بدل المساحات السوداء.
- تحسينات هاتف 320px للرأس والإشعارات وصفحات الإرشاد، مع منع اختفاء الشعار أو خروج الأزرار من الشاشة.
- طبقة الإصدار في `assets/styles/khadamati-v62.css` وService Worker جديد يمنع بقاء النسخة القديمة على الأجهزة المثبت عليها التطبيق.

### الأساس المستمر من v58

- مكتبة رسوم دلالية مخصصة في `assets/scripts/khadamati-visuals-v56.js` تستبدل رمز الصيانة المكرر برسومات مستقلة للأقسام والخدمات، وتُستخدم تلقائياً في الرئيسية والبحث والطلب ولوحة الإدارة.
- محرر الأقسام والخدمات يتضمن منتقي رسوم موحداً؛ أي تعديل من الإدارة يظل متناسقاً في جميع شاشات التطبيق ولا يحتاج كتابة Emoji أو كود SVG.
- مركز تقارير تشغيلي جديد يجمع الطلبات والتحويل والجمهور والاشتراكات والمال والجودة، مع CSV عربي سليم، وWord حقيقي، وطباعة/حفظ PDF بالعربية RTL والإنجليزية LTR.
- مسار الطلب المباشر أصبح هاتفياً بالكامل: ثلاثة أقسام أو خدمات في الصف، انتقال تلقائي بعد اختيار الخدمة، ملخص للاختيار مع رجوع آمن، وأزرار داخل تدفق الصفحة لا تغطي المحتوى.
- بطاقة مراجعة الطلب تملأ شبكة المعلومات بأربع خلايا متوازنة، وتعرض عدد المزودين المطابقين مع الموقع والأولوية والموعد.
- تسجيل المزود يطلب تاريخ انتهاء السجل أو الرخصة عند إنشاء الحساب، مع بقاء طلبات التسجيل القديمة قابلة للمراجعة دون فقد البيانات.
- محرر صور موحد وحديث يدعم السحب والتكبير والتصغير والتدوير والقص والمعاينة والحذف، ويُستخدم للصور الشخصية وصور الأعمال والإعلانات.
- بطاقات المزودين في البحث تعرض الصورة كاملة خلف تدرج شفاف مقروء، مع حالة ومفضلة واضحتين ومن دون مساحات بيضاء أو نص خارج البطاقة.
- مركز إشعارات الإدارة يحافظ على الإجراءات داخل عرض الهاتف، ويعرض زر الحذف بوضوح بدل المربع الفارغ.
- طبقات التصميم `v57` و`v58` مع تصحيحات `v59` تحافظ على شبكة 8px والتباين والوضع الداكن وتدعم الهاتف من عرض 320px.
- بوابة بداية تملأ شاشة الهاتف بصورة متوازنة، ورأس تطبيق مضغوط يحافظ على اسم وشعار «خدماتي» من دون قص أو فراغات كبيرة.
- شاشة الخدمات تستخدم شريط أقسام أفقيًا قابلًا للسحب وبطاقات ثلاثية ثابتة؛ لا تتداخل العناوين ولا تختفي خلف شريط التنقل.
- بطاقات المزودين أصبحت بطاقات قرار مختصرة تحافظ على مساحة الصورة وتعرض المعلومات والإجراء بترتيب بصري واضح.
- بوابة المزود تعرض أربعة مسارات أساسية وشاشة «المزيد»، مع لوحة يومية تبرز الطلب التالي وحالة الملف والموعد بدل تكديس الأدوات.
- لوحة الإدارة تكيفية على الهاتف: خمسة أقسام رئيسية ومركز أدوات مجمّع لبقية العمليات والمال والإعدادات، مع تثبيت عداد التنبيهات على أيقونته.
- المحادثة بملء شاشة الهاتف، وتحديث إرسال الرسائل موضعي وفوري من دون إعادة بناء النافذة أو فقد الصورة والصوت والموقع الجاري إرفاقه.
- الأيقونات مبنية على نظام التطبيق بدل الرموز النصية، مع أهداف لمس 44px ومراعاة `safe-area` والاتجاهين RTL/LTR والوضع الداكن.
- Service Worker باسم cache جديد وملفات v59 ضمن App Shell ليصل الإصدار إلى الأجهزة المثبت عليها التطبيق بدل بقاء واجهة قديمة.
- اختبارات الهاتف والكمبيوتر تغطي المستخدم والطلب والمزود والإدارة والمحادثة والخرائط والاشتراكات ورسوم الخدمات والتقارير، واختبار الأداء يبدأ خادمه المعزول تلقائيًا.

## المكونات

- واجهة تطبيق ويب تقدمي في `index.html` و`public/index.html`.
- خادم Python في `server.py` وطبقة نطاق في `khadamati_domain.py`.
- SQLite افتراضياً مع مخطط PostgreSQL موثق للتوسع.
- Data Adapter يحافظ على توافق بيانات LocalStorage القديمة ويعزل الزائر عن جلسات المستخدم والمزود والإدارة.
- مطابقة دقيقة حسب القسم والخدمة والمنطقة والتوفر، على موجتين: أفضل 5 ثم التوسع إلى 10 عند الحاجة.
- محادثات وإشعارات مجمعة وطلبات وعروض وموافقات تواصل مرتبطة بالحساب والطلب الصحيحين.
- ساحة طلبات عامة آمنة، مع ترشيح مزود مطابق من مستخدم آخر؛ لا يُخطر المزود إلا عندما يختاره صاحب الطلب ويرسل الطلب إليه.
- سياسات خصوصية وجودة وشروط وإلغاء بالعربية والإنجليزية. البريد المركزي: `om.khadamati@gmail.com`.
- خريطة صيانة الكود ومسارات التطوير الآمنة موثقة في `DEVELOPER_GUIDE_AR.md`، وتقارير الجاهزية الحالية داخل `ملفات-الإطلاق/`، والبرومبت التنفيذي الكامل في `KHADAMATI_MASTER_TRANSFORMATION_PROMPT_AR.md`.

## الباقات الإنتاجية

لا توجد إلا الخطط الخمس التالية، والعملـة `OMR`:

| المفتاح | المدة | السعر | أبرز الصلاحيات |
|---|---:|---:|---|
| `foundation_12m` | 12 شهراً | 0 | فرد: 3 خدمات/قسم واحد، شركة: 6 خدمات/3 أقسام، وصول بعد 15 دقيقة |
| `basic_6m` | 6 أشهر | 6 | فرد: 6 خدمات/قسمان، شركة: 8 خدمات/4 أقسام، وصول بعد 10 دقائق |
| `basic_12m` | 12 شهراً | 10 | صلاحيات الأساسي نفسها لمدة سنة |
| `professional_12m` | 12 شهراً | 20 | فرد: 10 خدمات/3 أقسام، شركة: 12 خدمة/4 أقسام، إشعار فوري وتقارير متقدمة |
| `business_12m` | 12 شهراً | 40 | فرد: 12 خدمة/4 أقسام، شركة: 20 خدمة/5 أقسام و3 أعضاء و3 فروع وصندوق مشترك |

الأسعار والصلاحيات والمدد قابلة للإدارة من الخادم. فترة السماح الافتراضية 14 يوماً، مع تنبيهات 30 و14 و7 و1 يوم ويوم الانتهاء. انتهاء الاشتراك يوقف الظهور واستقبال الطلبات ولا يحذف الحساب أو المحادثات.

## الخصوصية والأمان

- لا يظهر هاتف العميل للمزود إلا بعد موافقة مستقلة للقناة والطلب والمزود عبر `ContactConsentService`.
- الخطة المدفوعة لا تمنح حق الوصول إلى بيانات العملاء.
- وثائق التحقق خاصة بالإدارة المخولة.
- لا توجد كلمات مرور أو رمز إدارة ثابت في JavaScript، ولا تعرض الإدارة الرموز النصية.
- الأسعار الموثوقة والتحقق من الدفع وWebhook تتم في الخادم.
- OTP في الإنتاج لا يعمل كرمز تجريبي؛ يلزم ربط قناة إرسال حقيقية.
- كل تعديل إداري أو مالي حساس يسجل في Audit Log.

## التشغيل المحلي

```powershell
$env:KHADAMATI_ENV="development"
$env:KHADAMATI_DEV_ADMIN_CODE="ضع-رمزاً-محلياً"
$env:KHADAMATI_DEV_OTP_CODE="123456"
python server.py
```

أو شغّل `start-khadamati-app.bat` على Windows، ثم افتح:

```text
http://127.0.0.1:8080
```

للهاتف على شبكة Wi-Fi نفسها افتح `http://IP-الجهاز:8080`. قد يلزم السماح لـPython في جدار حماية Windows.

## متغيرات البيئة

استخدم `.env.example` مرجعاً. أهم القيم الإنتاجية:

- `KHADAMATI_ADMIN_CODE`: رمز إدارة قوي وغير محفوظ في المستودع.
- `KHADAMATI_OTP_PEPPER`: سر عشوائي لتجزئة رموز OTP.
- `KHADAMATI_MEDIA_SIGNING_KEY`: سر مستقل لتوقيع روابط الوثائق والوسائط الخاصة.
- `KHADAMATI_LOGIN_MAX_ATTEMPTS`, `KHADAMATI_LOGIN_LOCK_MINUTES`: حدود قفل محاولات الدخول.
- `KHADAMATI_MEDIA_URL_TTL_SECONDS`: عمر رابط الوسائط الخاصة الموقّع.
- `KHADAMATI_MAX_JSON_BYTES`: الحد الأعلى لجسم JSON قبل رفضه.
- `KHADAMATI_DB_PATH`, `KHADAMATI_UPLOAD_DIR`, `KHADAMATI_BACKUP_DIR`: مسارات تخزين دائمة.
- `KHADAMATI_PUBLIC_URL`: رابط GitHub Pages النهائي.
- `KHADAMATI_ALLOWED_ORIGINS`: الأصول المسموح بها.
- `KHADAMATI_SUPPORT_EMAIL`: بريد التواصل المركزي، وقيمته الافتراضية `om.khadamati@gmail.com`.
- `KHADAMATI_PAYMENT_*`: إعدادات Payment Adapter وWebhook.
- `WHATSAPP_*`: WhatsApp Cloud API الاختياري.
- `VAPID_*`: إشعارات الويب الاختيارية.

لا يصبح الدفع أو OTP إنتاجياً بمجرد إظهار الأزرار. يجب توفير مزود دفع/رسائل، أسرار صحيحة، HTTPS، واختبار Webhook الخادمي.

## Migration

قبل ترحيل الاشتراكات ينشئ الخادم نسخة SQLite احتياطية. الترحيل يعمل مرة واحدة بالمفتاح:

```text
KHADAMATI_SUBSCRIPTION_MIGRATION_V1
```

تُعطل الخطط القديمة بدلاً من حذف سجلاتها، وتُحفظ الاشتراكات والمدفوعات والتواريخ. لا تحذف قاعدة البيانات أو ملفات النسخ الاحتياطية أثناء الترحيل.

## النسخ الاحتياطي والاستعادة

أنشئ النسخة في مسار آمن خارج مجلد النشر المؤقت:

```powershell
python scripts/backup_sqlite.py --database khadamati.sqlite3 --uploads public/uploads --output backups/khadamati-backup.zip
```

يستخدم الأمر SQLite Backup API، ويضيف Manifest وبصمات SHA-256 لقاعدة البيانات والملفات. جرّب الاستعادة دائمًا إلى مسارات جديدة ومعزولة:

```powershell
python scripts/restore_sqlite_backup.py --archive backups/khadamati-backup.zip --database-target restored/khadamati.sqlite3 --uploads-target restored/uploads --confirm RESTORE_TO_EMPTY_TARGET
```

أداة الاستعادة ترفض الكتابة فوق قاعدة أو مجلد موجودين. لا تُجرّب الاستعادة فوق بيانات العمل الحقيقية.

## الخريطة والتوفر وحدود الباقات

- الخريطة التفاعلية تستخدم Leaflet وOpenStreetMap، ولا تحتاج مفتاح API سرياً.
- لا تُنشأ إحداثيات افتراضية عند تعذر الموقع، ولا يظهر مزود على الخريطة من دون إحداثيات صحيحة وموافقة `mapVisible`.
- المزود العام في الخريطة والبحث يجب أن يكون معتمداً ونشطاً ومتاحاً وتسمح باقته بالظهور واستقبال الطلبات.
- المصدر الموحد لحدود الخدمات والأقسام والولايات هو كتالوج الباقات في الخادم. الواجهة تعرض الاستهلاك، لكن الخادم يعيد التحقق ويمنع التجاوز عبر API.
- عند خفض الباقة تُحفظ البيانات القديمة ولا تُحذف؛ يُمنع فقط إضافة عناصر جديدة حتى يصبح الملف ضمن الحد أو تتم الترقية.
- حالة توفر القسم أو الخدمة في الطلب المباشر تأتي من بيانات المزودين المؤهلين، ولا تسمح بمتابعة خدمة لا يوجد لها مزود متاح.

## النشر

`render.yaml` يجهز خادم Python. في Render أضف أسرار الإدارة وOTP والدفع يدوياً، واضبط قرصاً دائماً أو PostgreSQL وتخزين ملفات دائم قبل الإطلاق العام.

قبل أي نشر إنتاجي شغّل فحص الجاهزية بعد تركيب مسارات التخزين، وهو يعرض أسماء
المتغيرات الناقصة ورموز الأخطاء فقط ولا يطبع قيم الأسرار:

```powershell
python scripts/check_production_readiness.py
```

[نشر أو استعادة خادم خدماتي على Render](https://render.com/deploy?repo=https://github.com/LLLX6/Khadamati)

الخادم المنشور للتطبيق هو `https://khadamati-app-api.onrender.com`. يمكن فحصه من `https://khadamati-app-api.onrender.com/api/bootstrap`. إذا أعاد الخادم حالة `503` مع عبارة `Service Suspended`، فخدمة Render نفسها متوقفة وليست المشكلة في الواجهة أو رقم الهاتف. افتح الخدمة من لوحة Render واختر **Resume**، أو أنشئها من رابط النشر أعلاه. إذا كان الإيقاف مفروضاً من Render على الخطة المجانية، يلزم نقل الخدمة إلى خطة مدفوعة وفق سياسة Render.

GitHub Pages يقدم الواجهة الثابتة وService Worker فقط. لا يستطيع وحده تشغيل قاعدة البيانات أو تسجيل المزود أو OTP أو الدفع أو إرسال Push؛ هذه الوظائف تحتاج رابط خادم HTTPS متاحاً للواجهة.

رابط الواجهة المتوقع:

```text
https://lllx6.github.io/Khadamati/
```

## الاختبارات

```powershell
python -m py_compile server.py khadamati_domain.py
python -m unittest discover -s tests -p "test_*.py" -v
python tests/security-api.py
python tests/run-smoke-isolated.py
python tests/performance-api.py
python scripts/verify_repository.py
python scripts/audit_database.py khadamati.sqlite3 --allow-demo-data
node tests/smoke-ui.js
node tests/performance-ui.js
```

لفحص أصغر هاتف والكمبيوتر غيّر `KHADAMATI_VIEWPORT_WIDTH` و`KHADAMATI_VIEWPORT_HEIGHT` قبل تشغيل اختبار الواجهة. تحقق الإصدار v64 إلزامياً من 320×740 و390×844 والكمبيوتر، ويغطي الطلب التلقائي، تبديل حساب المستخدم والمزود، حدود التسجيل، محرر الصور، معرض الأعمال، التقارير، إشعارات الإدارة، حفظ الجلسة، المحادثة والخريطة.

اختبار الواجهة يبدأ خادماً ثابتاً معزولاً تلقائياً. لاختبار API شغّل خادم اختبار بقاعدة منفصلة في نافذة أولى:

```powershell
$env:KHADAMATI_ENV="test"
$env:KHADAMATI_ADMIN_CODE="رمز-اختبار-قوي"
$env:KHADAMATI_DB_PATH="$PWD\tests\__smoke.sqlite3"
$env:KHADAMATI_UPLOAD_DIR="$PWD\tests\__smoke-uploads"
python server.py
```

ثم في نافذة ثانية:

```powershell
$env:KHADAMATI_TEST_ADMIN_CODE="رمز-اختبار-قوي"
python tests/smoke-api.py
```

الاختبارات تغطي المستخدم والمزود والإدارة والطلب والمطابقة والمحادثة والموافقة والخرائط والهاتف، كما تتحقق من رفض التلاعب بمبلغ الاشتراك.
تفاصيل التهديدات والإصلاحات ونتائج الأدوات موثقة في `SECURITY_AND_QUALITY_AUDIT.md`.

## التوسع إلى PostgreSQL

راجع `POSTGRESQL_MIGRATION.md` و`postgres_schema.sql`. يمكن تصدير البيانات عبر:

```powershell
python export_khadamati_data.py
```
