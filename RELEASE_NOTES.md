# خدماتي v1.3.0 / r1 — مرشح الترجمة الخماسية

يدعم هذا المرشح العربية والإنجليزية والهندية والبنغالية والأوردو من منتقي لغة
واحد للزائر والمستخدم والمزود. العربية والأوردو تعملان من اليمين إلى اليسار،
واللغات الثلاث الأخرى من اليسار إلى اليمين. الترجمة مدمجة وثابتة داخل الإصدار؛
لا تُرسل نصوص الواجهة أو رسائل الأشخاص إلى خدمة ترجمة أثناء الاستخدام.

- أضيف قاموس مركزي واتجاه ومحلية وتخزين دائم لاختيار اللغة.
- شملت الترجمة الواجهات والحالات والأخطاء والإشعارات والتقارير المحلية والمشاركة.
- أضيف Manifest مستقل لكل لغة، وأضيفت الأصول إلى ذاكرة PWA ومرايا `public/`.
- يحتفظ التطبيق بالأسماء والرسائل والأوصاف التي يكتبها الأشخاص بلغتها الأصلية
  حتى لا يغيّر معناها أو يرسل بيانات خاصة إلى طرف ترجمة خارجي.
- يجب أن تراجع جهة لغوية بشرية النصوص القانونية والتسويقية قبل اعتمادها النهائي.

## سجل الإصدار السابق: v1.2.0 / r1

هذا المستند يصف المرشح الموجود في المستودع. لا يعني أن `v1.2.0` أصبح منشورًا
على GitHub Pages أو Render، ولا أن الخطة المدفوعة أو القرص الدائم فُعّلا. تُحدّث
هذه الحالة فقط بعد نجاح GitHub Actions والتحقق من الروابط العامة نفسها.

## ما يحله هذا الإصدار

- يوحّد رقم الواجهة والخادم والـmanifest وعامل الخدمة وإعداد Render على
  `1.2.0`، مع build للواجهة والـPWA باسم `r1`.
- يجعل الخادم مصدر الحقيقة للعمليات المشتركة، ويمنع واجهة المستخدم من إعلان
  النجاح قبل أن يؤكد الخادم حفظ العملية.
- يحفظ الطلب الذي لم يجد مزودًا كطلب ينتظر مزودًا مناسبًا بدل إظهاره كنجاح
  مكتمل أو إسقاطه، مع إعادة فحصه عند اعتماد مزود مؤهل.
- ينقل سجلات المالية والرعاية والقسائم إلى سجلات خادمية قابلة للتدقيق والتحقق
  من الصلاحيات بدل الاعتماد على حالة المتصفح وحدها.
- يضيف حالات عربية وإنجليزية واضحة: تم الحفظ، بانتظار المزامنة، وفشل الإرسال،
  ويحسن تنظيم مركز قرارات الإدارة وتفاصيل الطلب على الهاتف.
- يوحّد بوابة اعتماد المزود في الخادم: رقم ترخيص/سجل وتاريخ صلاحية ووثيقتان،
  ويعيد أي حساب غيّر دليل هويته إلى المراجعة قبل الظهور أو استقبال الطلبات.
- يعيد فحص حالة المزود واشتراكه وخدمته ومنطقته ووقت عمله وسعته وحظره عند
  تحرير الإرسال المؤجل وعند القبول، بما يمنع قبول طلبين عند سعة يومية واحدة.
- يؤخر رسائل القبول وحذف الوسائط حتى نجاح Commit، ويفصل صلاحيات عمليات الإدارة
  القانونية والمالية والمخاطر ومفاتيح Enterprise.

## سلامة PWA والنسخة غير المتصلة

- اسم cache هو `khadamati-app-shell-v1.2.0-r1` في نسختي عامل الخدمة
  المتطابقتين.
- إذا فشل تحميل أي ملف أساسي أثناء التثبيت، لا تُرقّى نسخة ناقصة ويستمر العامل
  السابق المكتمل.
- لا يحفظ عامل الخدمة ردًا مكان `index.html` إلا إذا كان HTML ناجحًا وغير مرفق
  ومن المسار الأساسي نفسه.
- لا يشمل الرجوع غير المتصل API أو الوسائط الخاصة أو الرفع أو ملفات التنزيل
  والتصدير وPDF وZIP وOffice.
- حذف caches القديمة محصور بمفاتيح خدماتي، وruntime cache مقصور على أصول ثابتة
  عامة من الأصل نفسه ولا تحمل `private` أو `no-store`.
- يُسمح فقط بمعامل cache-busting المسمى `v` وبقيمة محدودة، ويُوحّد إلى مسار
  ثابت؛ لذلك تعمل ملفات CSS وJavaScript ذات رقم الإصدار عند انقطاع الشبكة دون
  إنشاء مفاتيح cache غير محدودة.

## التخزين والتشغيل على Render

- يتابع Blueprint فرع `release/production-readiness` ويعطل النشر التلقائي.
- يستخدم فحص الجاهزية `/readyz` بدل اعتبار استجابة `/healthz` وحدها كافية.
- يجمع قاعدة SQLite والرفع والنسخ الاحتياطية تحت قرص واحد في
  `/var/data/khadamati` بسعة ابتدائية 1GB.
- يحدد Blueprint نوع الخدمة `starter`. وفق وثائق Render، القرص الدائم يحتاج
  خدمة مدفوعة، بينما الملفات المحلية وSQLite في الخدمة المجانية تضيع عند
  إعادة التشغيل أو إعادة النشر. المراجع الرسمية:
  [Persistent Disks](https://render.com/docs/disks) و
  [Free Web Services](https://render.com/docs/free).

## بوابات الإطلاق

لا يُعلن هذا المرشح إصدارًا حيًا قبل تحقق جميع الآتي:

1. نجاح فحص Python وJavaScript واختبارات الوحدة والأمان وAPI والواجهة.
2. تطابق `index.html` وmanifest وعامل الخدمة والأصول المشتركة مع مرايا `public`.
3. أخذ نسخة احتياطية من البيانات الحالية قبل أي تغيير استضافة أو ترحيل.
4. تطبيق Blueprint والقرص الدائم والأسرار من حساب المالك، ثم نجاح `/readyz=200`.
   يشمل ذلك مفتاح المصادقة الثنائية، تسجيل مدير محمي، ومفاتيح Web Push إذا
   ستُعلن إشعارات الهاتف متاحة.
5. تحقق أن الواجهة العامة وAPI يعلنان `v1.2.0` من Commit واحد.
6. اعتماد مزودين حقيقيين لخدمات ومناطق محددة؛ بيانات العرض تبقى معطلة في
   الإنتاج ولا تُستخدم لتزييف توفر السوق.
7. فحص رحلة مستخدم ومزود وإدارة على هاتف حقيقي بالعربية والإنجليزية.
8. إنشاء نسخة احتياطية خارج قرص Render نفسه، وتنفيذ اختبار استعادة معزول
   ومقارنة أعداد السجلات قبل فتح الحركة.

## ما لم يحدث بعد

- لم يُدمج هذا المرشح في `main`؛ قواعد المشروع تمنع ذلك.
- لم يحدث نشر عام أو تغيير DNS أو تفعيل خطة مدفوعة من خلال هذه التعديلات.
- لم تُحذف بيانات أو حسابات، ولم تُرفع قاعدة بيانات أو ملفات مستخدمين إلى Git.
- تفعيل القرص والخطة والأسرار، والتحقق من مزودين حقيقيين، إجراءات تشغيل خارجية
  يجب إثباتها على البيئة العامة قبل وصف الإصدار بأنه جاهز أو حي.
